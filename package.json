{
  "name": "pig-sandbox",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "ブラウザで遊べるアメーバピグ風の箱庭ゲーム",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vitest run && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/three": "^0.186.0",
    "typescript": "^5.9.3",
    "vite": "^6.3.6",
    "vitest": "^3.2.7"
  }
}
